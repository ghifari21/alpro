#include <stdio.h>

int main(){
    int i, j, n, m, p, r;
    int hasil=0;
    scanf("%d", &n);
    scanf("%d", &m);
    scanf("%d", &p);
    scanf("%d", &r);

    if (r < 3)
    {
        hasil = (n/2) * (m * r) + n +;
    }
    printf("%d\n", hasil);
    return 0;
}